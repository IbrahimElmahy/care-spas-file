# دليل موقع عرض مشروع ستايلك (Stylek - Showcase)

تم تصميم موقع تفاعلي (Showcase) لعرض شاشات تطبيق "ستايلك" (Stylek) بشكل كامل وجذاب، مع دعم اللغة العربية وتجربة مستخدم سلسة.

## 📂 بنية الملفات
الملفات موجودة في مجلد `showcase/`:

- **`index.html`**: الهيكل الرئيسي للموقع. يحتوي على جميع الشاشات (40 شاشة) مرتبة منطقياً حسب رحلة المستخدم، مع نصوص عربية وتنسيق (RTL).
- **`style.css`**: ملف التنسيقات. تم تحديثه ليدعم العربية، وتم **إزالة إطار الهاتف** لعرض الصور بوضوح كامل وبحجم مناسب.
- **`watermark.png`**: صورة شعار "حلول البرمجة الذكية" المستخدم كعلامة مائية متكررة في خلفية الموقع.
- **`script.js`**: معالج التفاعل (Scrollytelling). يقوم بتغيير الصور تلقائياً عند التمرير بين النصوص.
- **`images/`**: مجلد يحتوي على جميع صور شاشات التطبيق المستخدمة في العرض، مما يجعل الموقع مستقلاً (Standalone) وسهل النشر.

## ✨ المميزات
- **إطار موبايل حديث**: عند التصفح من الهاتف، تظهر الصور داخل إطار موبايل أنيق (Bezel) بمسافة داخلية (Padding) وزوايا دائرية، مما يعطي انطباعاً واقعياً للتطبيق.
- **عرض كامل للصور**: تم إزالة الحواف (Bezels) على الكمبيوتر للتركيز الكامل على تصميم الـ UI، بينما تظهر على الموبايل لتجربة مستخدم متناسقة.
- **علامة مائية مخصصة**: شعار الشركة يظهر كخلفية متكررة (Watermark) لضمان حقوق الملكية والهوية البصرية.
- **تفاعلية (Scrollytelling) على الكمبيوتر**: الصور تتغير بسلاسة مع قراءة وصف كل شاشة.
- **دعم العربية**: جميع العناوين والنصوص باللغة العربية مع خط "Cairo" المتميز.
- **شامل**: يغطي جميع الـ 40 شاشة (البداية، التسجيل، الرئيسية، الحجز، الدفع، الملف الشخصي، إلخ).
- **سهل النشر**: لا يتطلب إعدادات معقدة، فقط ارفع مجلد `showcase` وسيعمل مباشرة.

## 🚀 كيفية المشاهدة
1. افتح المجلد `showcase` داخل مجلد المشروع.
2. شغل ملف `index.html` في المتصفح.

## 📦 النشر (Deployment)
المشروع جاهز للنشر على GitHub Pages أو Vercel. فقط ارفع المجلد `showcase` أو المستودع كاملاً ووجه الرابط إلى `showcase/index.html`.
